#ifndef ADMIN_H
#define ADMIN_H
#include"Account.h"
class Admin:public Account
{
public:
    Admin();
    Admin(string,string,string,string,long long,bool);
    //Add and delete of ittems funtion must be added adter makih class
    
};
#endif